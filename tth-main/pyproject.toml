[project]
name = "zkp-snark-ver"
version = "0.1.0"
description = "Secure transfer simulation using zk-SNARKs"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.109",
    "uvicorn[standard]>=0.23",
    "requests>=2.31",
    "pydantic>=2.5",
    "python-dotenv>=1.0",
    "watchfiles>=0.21",
]

[tool.uvicorn]
factory = false
reload = true
